# API Registry Audit — 2026-09-01

Статус: research branch only. Production не изменён.

## Подтверждённые снимки источников

### Wildberries

Пинован вторичный снимок `MissiaL/wildberries-api` commit `6f56592666e423bf587da06fe204d287094c3a38`.

Его manifest указывает:

- 13 официальных страниц OpenAPI;
- 286 операций;
- browser-redoc-openapi extraction;
- capture/rate-limit verification time: 2026-08-19T18:12:37.448Z.

Это не заменяет официальный `dev.wildberries.ru`, но даёт воспроизводимый snapshot для машинного diff, пока официальный портал блокирует plain HTTP anti-bot проверкой.

### Ozon

Пинован вторичный снимок `MissiaL/ozon-api` commit `1953152c36955225b459cf55963a2c3a7a234661`.

Снимок заявляет:

- Seller API: 463 операции / 57 разделов;
- Performance API: 48 операций / 6 разделов;
- источник сборки — браузерная выгрузка официального Ozon Swagger.

Официальный браузерный export остаётся обязательным источником перед финальным production-переходом на спорных/критичных методах.

## Критический итог текущего аудита

На текущем production snapshot `07a3408eab9a2abe4ba1e9dbcfdd8c67bed0458d` уже подтверждено **16 исполняемых operation_id**, которые относятся к отключённым или официально удалённым из документации версиям API:

- Wildberries: 2 operation_id;
- Ozon: 14 operation_id.

Это не означает, что все 16 нужно немедленно физически удалить из YAML. Правильный первый production-шаг — сделать их недоступными для generic MCP selection с понятным `retired_endpoint` и указанием замены. Исторические записи можно оставить как provenance до завершения миграционных тестов.

Дополнительно 2 Ozon Finance v3 operation_id имеют уже объявленную дату отключения 2026-09-08 и требуют миграции до этой даты.

Полный кандидат-блоклист хранится в `api_registry/catalog/retired_endpoint_blocklist.yaml`.

## Уже найденные расхождения production-каталога

### 1. Wildberries: отключённые Statistics/Finance endpoints всё ещё исполняемы

В production присутствуют:

- `wb_stats_stocks` → `GET /api/v1/supplier/stocks`, отключение 2026-06-23;
- `wb_report_realization` → `GET /api/v5/supplier/reportDetailByPeriod`, отключение 2026-07-15.

При этом в том же production-каталоге уже присутствуют новые замены:

- `wb_post_api_analytics_stocks_report_wb_warehouses` → `POST /api/analytics/v1/stocks-report/wb-warehouses`;
- `wb_post_api_finance_sales_reports_list` → `POST /api/finance/v1/sales-reports/list`;
- `wb_post_api_finance_sales_reports_detailed` → `POST /api/finance/v1/sales-reports/detailed`;
- `wb_post_api_finance_sales_reports_detailed_reportid` → `POST /api/finance/v1/sales-reports/detailed/{reportId}`.

Следовательно, старые WB методы не нужны как generic fallback и должны быть lifecycle-blocked после production review.

### 2. Ozon posting versions, отключённые 31.08.2026

Production всё ещё содержит:

- `ozon_fbs_unfulfilled` → `/v3/posting/fbs/unfulfilled/list`;
- `ozon_fbs_list` → `/v3/posting/fbs/list`;
- `ozon_fbo_list` → `/v2/posting/fbo/list`;
- `ozon_post_v1_posting_digital_list` → `/v1/posting/digital/list`.

Официальный Ozon Seller API notification channel назначил отключение всех этих версий на 2026-08-31.

Новые пути:

- `/v4/posting/fbs/unfulfilled/list`;
- `/v4/posting/fbs/list`;
- `/v3/posting/fbo/list`;
- `/v2/posting/digital/list`.

В проверенном production snapshot эти четыре replacement path отсутствуют. Это CRITICAL coverage gap.

### 3. Ozon старые Warehouse/Stocks версии

Production содержит старые версии, отключённые 2026-04-07:

- `/v1/product/info/stocks-by-warehouse/fbs`;
- `/v1/delivery-method/list`;
- `/v1/warehouse/list`.

Хорошая новость: в том же каталоге уже существуют их v2 replacements. Поэтому это в первую очередь задача lifecycle blocking / routing cleanup, а не разработка нового покрытия.

### 4. Ozon Chat v2

`ozon_list_4` всё ещё указывает на `/v2/chat/list`, который Ozon 2026-06-09 удалил из документации и заменил на `/v3/chat/list`.

`/v3/chat/list` уже присутствует в production как `ozon_post_v3_chat_list`.

Одновременно найден дефект safety-инвентаризации: старый auto-imported `/v2/chat/list` помечен `write`, тогда как актуальный `/v3/chat/list` — read. Это подтверждает, что safety нельзя выводить из факта HTTP POST.

### 5. Ozon старый FBO Draft flow

В production остаются v1 методы:

- `/v1/draft/create`;
- `/v1/draft/create/info`;
- `/v1/draft/timeslot/info` (два operation_id);
- `/v1/draft/supply/create`;
- `/v1/draft/supply/create/status`.

Ozon объявил их отключёнными с 2026-03-16. Большая часть v2 replacement flow уже присутствует в project catalog, поэтому старые operation_id должны быть исключены из generic selection.

### 6. Ozon Finance v3 — ближайший дедлайн

Production содержит:

- `/v3/finance/transaction/list`;
- `/v3/finance/transaction/totals`.

Официальный Ozon Seller API notification channel объявил отключение 2026-09-08 и переход на семейство accrual API:

- `/v1/finance/accrual/postings`;
- `/v1/finance/accrual/types`;
- `/v1/finance/accrual/by-day`.

Это не one-to-one замена. До миграции надо доказать семантику расчёта итогов и обработку неизвестных типов начислений. На текущем snapshot `/v1/finance/accrual/by-day` в production-каталоге отсутствует.

### 7. Ozon auto-imported UNVERIFIED operations

В production-каталоге имеется большой блок методов с комментарием `auto-imported from go-client; UNVERIFIED`.

Риск двойной:

1. request/body contract может не совпадать с актуальным Swagger;
2. `safety` местами нельзя выводить из HTTP POST. В Ozon чистые read-операции часто являются POST.

До OpenAPI-сверки эти записи должны считаться implementation inventory, а не подтверждённым API contract.

## Wildberries Orders — оптимизация подтверждена как кандидат

Для `GET /api/v1/supplier/orders` зафиксировано:

- 1 request / 60 sec / seller account / method;
- история до 90 дней;
- обновление приблизительно каждые 30 минут;
- `flag=0` выбирает по `lastChangeDate >= dateFrom`;
- до 80 000 строк до продолжения pagination.

Следствие: пользовательский запрос на несколько близких календарных дней потенциально можно выполнить одним upstream WB request и сгруппировать внутри MCP по фактической дате заказа. Обязательное условие — доказать полноту ответа и обработать 80k boundary.

## Создан механизм нормализации

`api_registry/build_catalog.py` строит детерминированный `api_registry/api_catalog.json` из:

- `wb_mcp/endpoints.yaml`;
- `ozon_mcp/endpoints.yaml`;
- `ozon_mcp/perf_endpoints.yaml`;
- проверенных `api_registry/overrides.yaml`.

Он считает endpoint inventory, SHA-256 исходных каталогов, safety breakdown и поддерживает `--check` для drift detection.

## Следующие безопасные шаги

1. Не менять production, пока технический Codex завершает текущую задачу limiter/range.
2. После нового canonical SHA синхронизировать research branch с новым HEAD.
3. Сгенерировать нормализованный `api_catalog.json` на актуальном canonical snapshot.
4. Построить автоматический diff implementation catalog ↔ pinned OpenAPI snapshots.
5. Подготовить отдельный production change: generic tool должен отклонять verified retired operation_id до отправки HTTP-запроса.
6. Отдельным изменением мигрировать отсутствующие Ozon v4/v3/v2 replacements и Finance accrual family.
